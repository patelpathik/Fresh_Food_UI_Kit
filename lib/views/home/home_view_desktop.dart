import 'package:flutter/material.dart';

class HomeDesktopPortrait extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Home Desktop"),
        ),
      ),
    );
  }
}
