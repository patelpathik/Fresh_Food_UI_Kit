import 'package:flutter/material.dart';

class ProductTabletDesktopPortrait extends StatelessWidget {
  const ProductTabletDesktopPortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("WIP..")));
  }
}
