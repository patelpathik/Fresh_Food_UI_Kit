import 'package:flutter/material.dart';

class StoreTabletDesktopPortrait extends StatelessWidget {
  const StoreTabletDesktopPortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
